apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
spec:
  template:
    spec:
      containers:
      - name: user
        image: weaveworksdemos/user
        ports:
        - containerPort: 80